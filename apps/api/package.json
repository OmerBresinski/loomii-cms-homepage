{
  "name": "@ai-cms/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/app.ts"
    }
  },
  "scripts": {
    "build": "prisma generate && bun build src/index.ts --outdir dist --target bun --minify",
    "predev": "lsof -ti:3001 | xargs kill -9 2>/dev/null || true",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run dist/index.js",
    "type-check": "tsc --noEmit",
    "lint": "eslint src",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "webhook:dev": "smee -u https://smee.io/e5Em1seoO14eJsC -t http://localhost:3001/webhooks/github"
  },
  "dependencies": {
    "@clerk/backend": "^2.27.0",
    "@hono/clerk-auth": "^3.0.3",
    "@hono/zod-validator": "^0.4.2",
    "@prisma/client": "^6.1.0",
    "ai": "^5.0.113",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "prisma": "^6.1.0",
    "typescript": "^5.7.2"
  }
}
